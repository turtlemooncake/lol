[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lol"
version = "1.0.0"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["src*", "config*", "services*", "util*", "strategies*", "jobs*"]